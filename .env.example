# 원격(Cloudflare 등) 저장소를 사용할 때만 설정.
# 설정하지 않으면 브라우저 localStorage에 저장됩니다.

# 원격 저장소 사용 (1 또는 true)
VITE_REMOTE_STORE=

# API 주소. 같은 도메인(Cloudflare Pages)에 배포하면 비워둡니다.
# 다른 도메인의 API를 쓸 때만 예: https://api.example.com
VITE_API_BASE=
